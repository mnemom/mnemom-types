/**
 * A2A trust extension types.
 *
 * Re-exports from reputation for convenience — the A2A trust
 * extension is defined alongside the reputation score.
 */

export type { A2ATrustExtension } from './reputation';
